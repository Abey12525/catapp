apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: catapp-abey
  name: catapp-abey
spec:
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: catapp
