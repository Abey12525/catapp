apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: catapp-abeyv001
  name: catapp-abeyv001
spec:
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: catapp-abeyv001
