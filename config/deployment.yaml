apiVersion: apps/v1
kind: Deployment
metadata:
  name: catapp-abey
  labels:
    app: catapp-abey
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catapp-abey
  template:
    metadata:
      labels:
        app: catapp-abey
    spec:
      containers:
        - name: catapp-abey
          # This image will be overwritten by the update-deployment Task
          image: catapp:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              protocol: TCP
