apiVersion: v1
kind: Service
metadata:
  labels:
    app: catapp-abeyv002
  name: catapp-abeyv002
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: catapp-abeyv002
