apiVersion: v1
kind: Service
metadata:
  labels:
    app: catapp-abeyv001
  name: catapp-abeyv001
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: catapp-abeyv001
